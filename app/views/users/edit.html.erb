<% content_for :title, "Editing user" %>

<div class="max-w-4xl mx-auto mt-10 p-6 bg-white rounded-lg shadow-md">
  <h1 class="text-2xl font-bold text-gray-800 mb-6">Editing User</h1>

  <%= render "form", user: @user %>

  <div class="mt-6 flex justify-between">
    <%= link_to "Show this user", @user, class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
    <%= link_to "Back to users", users_path, class: "text-gray-600 hover:text-gray-800 font-medium" %>
  </div>
</div>